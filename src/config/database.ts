// =====================================================
// GroceryMind - Database Configuration (Browser-side)
// =====================================================

import { query, queryOne } from '@/utils/database';

export { query, queryOne };